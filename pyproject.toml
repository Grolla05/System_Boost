[project]
name = "wincleaner"
version = "0.1.0"
description = "Minimalist CLI utility to clean Windows temporary files"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
